import { ForgotPasswordPipe } from './forgot-password.pipe';

describe('ForgotPasswordPipe', () => {
  it('create an instance', () => {
    const pipe = new ForgotPasswordPipe();
    expect(pipe).toBeTruthy();
  });
});
